image: map.pgm
resolution: 0.05  # 每个像素代表的实际距离（单位：米）
origin: [0.0, 0.0, 0.0]  # 地图左下角在世界坐标系中的位置 [x, y, yaw]
negate: 0  # 0 表示黑色为障碍物，1 表示白色为障碍物
occupied_thresh: 0.65  # 占据阈值（0~1），高于此值的像素被认为是障碍物
free_thresh: 0.196  # 空闲阈值（0~1），低于此值的像素被认为是可通行区域